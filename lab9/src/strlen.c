#include <stdio.h>

int str_len(char *s) {
    int len = 0;

    // write the code to compare the two strings here

    return len;
}

int main() {
    char str[] = "HELLO";
    int length = str_len(str);
    printf("length = %i\n", length);
    return 0;
}
